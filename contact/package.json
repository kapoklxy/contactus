{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A simple static site",
  "scripts": {
    "build": "cp -r ./contact/. ./"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}